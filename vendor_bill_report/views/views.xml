<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record model="ir.actions.act_window" id="wizard_vendor_bill_action">
        <field name="name">Vendor Bill Report</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">vendor.bill</field>
        <field name="context">{}</field>
        <field name="target">new</field>
        <field name="domain">[]</field>
        <field name="view_mode">form</field>
    </record>
    <menuitem
            id="wizard_vendor_bill_report_root"
            name="Contractor Wages Report"
            action="wizard_vendor_bill_action"
            parent="account_reports.account_reports_partners_reports_menu"
            sequence="1"

    />

</odoo>